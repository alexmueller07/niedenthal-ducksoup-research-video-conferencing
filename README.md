Hi Ben,

This is the Mac version of the lab video call app.

## 1. Grab the app file

1. Go to this page: **https://github.com/alexmueller07/niedenthal-ducksoup-research-video-conferencing/actions**
2. Click the most recent **"Build macOS app"** run at the top that has a **blue or green check** next
   to it (if its yellow for some reason just wait a few minutes).
3. Scroll all the way to the bottom of that page. Under **Artifacts**, click
   **`lab-video-call-macos`**. It'll download as a `.zip`.
4. Double-click the `.zip` to unzip it. Inside you'll find
   **`Lab-Video-Call-3.0.0-universal.dmg`** — that's the installer.

## 2. Install it

1. Double-click the `.dmg` file. A little window pops open with the app icon next to an
   **Applications** folder.
2. Drag the **Lab Video Call** icon onto the **Applications** folder or like onto the desktop it dont really matter like anywhere on the system. That's it, its installed. 

And when opening you might have to do right click -> open instead of double click just bc its from a unidentified developer. Its not a virus pinky promise.


And then from there im sure you can figure it out pretty straight forward im sure you can figure it out. 

Ctrl + shift + q = Escape to allow you to type Confirm to close the app. Only way to close the app so like just do this. 
user: admin 
pass: admin 
for admin dashboard 
anything else or blank is participant screen
Must have 2 participants + admin to start the thing. 

If you have any questions lmk.

Alex
